# Terraform workspace

This folder is reserved for infrastructure-as-code resources for provisioning the EKS cluster and supporting networking.

Suggested next step:
- Add Terraform configuration for the VPC, EKS cluster, and node group
- Store environment-specific values in variables or CI/CD secrets
- Keep sensitive values out of version control
